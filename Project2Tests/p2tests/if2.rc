function : void main()
{
    bool x = true;
    bool y = false;

    if( x && y ) {}
    if( y && x ) {}    // Should short circuit
    if( x || y ) {}    // Should short circuit
    if( y || x ) {}
    if( !x )     {}
}
